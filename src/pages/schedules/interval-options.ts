export const acceptedHoursOptions = [1, 2, 3, 4, 6, 12].map(
  (value) => ({
    label: value,
    value,
  }),
);

export const acceptedMinutesOptions = [1, 2, 3, 4, 5, 10, 15, 20, 30].map(
  (value) => ({
    label: value,
    value,
  }),
);
